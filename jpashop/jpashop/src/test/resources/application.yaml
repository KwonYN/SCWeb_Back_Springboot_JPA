# docs.spring.io/spring-boot/docs/ ... 매뉴얼 참고
spring:
#  datasource:    # 이런 설정 없어도.. SpringBoot는 기본적으로 테스트를 In-Memory로 돌림ㅋ
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop  # test 종료 후에 완전히 drop 시켜 깔끔하게 상태 유지 가능!
#    properties:
#      hibernate:
##        show_sql: true        # stdout으로 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug  # JPA과 hibernate가 남기는 SQL을 모두 볼 수 있음(로그로 출력)
    org.hibernate.type: trace
